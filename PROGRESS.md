# 项目进度存档

最后更新：2026-09-13

> 判定逻辑的规则口径见 [docs/verification_logic.md](docs/verification_logic.md)，
> 本文件只记进度。
>
> 按时间顺序追加，后面的小节会推翻前面的结论 —— 第三节到第七节停在
> 2026-08-29 的"等票面"状态，第八节之后才是丹东数据到位后的实况。

## 一、当前状态一句话

基线定为 **FSMN-VAD + 不做碎段合并 + Paraformer-Large fp32 + 识别后吸附**，
丹东站离线端到端跑通（`src/offline/batch_verify.py`），评估过的其余方案连同
代码一并删除（第十、十一节）。在此基础上修掉了切分粒度造成的误判，碎段合一的
兜底挪到对齐层做（第十二节）。系统仍是原型：全流程只有三段录音共 27 条票面，
阈值未用标注数据标定，输出必须保留人工复核通道。

## 二、运行环境

| 项 | 值 |
|---|---|
| conda 环境 | `asr_dianwang`（Python 3.10，`D:\Anaconda\envs\asr_dianwang`） |
| torch | **2.11.0+cu128**（已从 2.13.0+cpu 换掉，`cuda_available=True`） |
| GPU | NVIDIA RTX 4060 Laptop，8GB 显存 |
| ASR 模型 | `models/SenseVoiceSmall/`，本地已下载，运行不联网 |

注意：`funasr` 对 torch 版本有兼容区间，重装 torch 后如果 import 报错，
优先怀疑 torch/torchaudio 与 funasr 的版本组合，而不是模型文件。

首次运行 VAD（`fsmn-vad`，约 1.7MB）和 CAM++ 声纹（约 7MB）需要联网从魔搭下载，
下载失败时代码会自动降级（VAD → 能量切分，声纹 → 纯能量判角色），不会中断。

## 三、已完成

### P0 基线实测
- `benchmarks/run_baseline.py`：批量跑目录、统计耗时与显存
- `transcribe.py`：单文件识别

### P1 离线校验内核
- `src/config.py`：YAML 配置加载
- `src/normalize/text_norm.py`：数字、单位、罗马数字归一（兼容 ITN 开/关两种模式）
- `src/normalize/pinyin.py`：模糊拼音编码 + 菏泽方言等价类
- `src/ticket/loader.py`、`src/ticket/slots.py`：票面加载与槽位抽取
- `src/verify/matcher.py`：槽位加权匹配 + 三档判定
- `src/verify/aligner.py`：顺序对齐状态机
- `tests/test_matcher.py`、`tests/test_text_norm.py`：含第 2/5 条方向对立、
  第 4/7 条电气/机械对立这两组对抗样例

### P2 分段与角色
- `src/asr/vad_stream.py`：FSMN-VAD 离线切分 + 流式增量，附能量兜底切分
- `src/speaker/role.py`：近远场能量 + CAM++ 声纹二类聚类 + 时序先验，
  含流式版 `OnlineRoleClassifier`

### 配置
- `configs/default.yaml`：阈值、槽位权重、VAD/角色/对齐/流式参数
- `configs/dialect_heze.yaml`：菏泽模糊音等价类
- `configs/lexicon.yaml`：领域词典

### 核对工具
- `scripts/inspect_audio.py`：对音频跑"分段 + 角色 + 识别"，不依赖操作票，
  用于人工核对 P2 效果。报告写到 `benchmarks/results/inspect/`

## 四、首轮实跑结论（2026-08-29，13 个 clip）

### 修掉的两个 bug
1. **fp16 从来没跑通过，而且根本不该开。** `model.half()` 只改权重，funasr 的
   特征提取那一路始终产出 fp32，编码器里的 Linear 和 FSMN 卷积会轮流报 dtype
   不匹配；funasr 自带的 `fp16=True` 是同样的写法，一样不能用。先改成
   `torch.autocast` 跑通，再实测发现 autocast 在这个模型上是**负收益**，
   于是默认关掉、回到 fp32。这个 bug 之前被 `warmup()` 里的
   `except Exception: return 0.0` 吞掉了，现在预热不再捕获异常。

   RTX 4060 / 13 个 clip / 111s 音频：

   | 精度 | 权重 | 峰值显存 | 峰值 reserved | RTF |
   |---|---|---|---|---|
   | fp32 | 907 MB | **965 MB** | 1046 MB | **0.0104** |
   | fp16 autocast | 907 MB | 1408 MB | 1432 MB | 0.0280 |

   识别结果 13 个 clip 里 12 个完全一致，唯一不同的那个 fp32 更准
   （"鉴定" vs "今定"）。原因是模型只有 234M 参数本来就不吃显存，autocast
   要额外缓存一份 fp16 权重副本，而每次 `generate` 都是新的 autocast 上下文，
   转换开销付了却用不上缓存。**结论：显存和速度都不是瓶颈，用 fp32。**
2. **时序先验会把角色标反。** 原实现只看前一段角色按"交替"填 UNKNOWN，
   完全不看本段能量。实测出现过能量倍率 1.51（明显近场）被判成唱票人的情况。
   已改成先验与能量倾向冲突时以能量为准。

### 观察到的现象
- **VAD 是有效的，能量兜底切分不可用。** VAD 跳过的空档（如 item_13 的
  2.3~11.1s）实测 RMS 均值 0.032，并非静音，但 VAD 判定为非语音 —— 设备绑在
  身上，走动时的衣物摩擦声能量很高却不是人声，VAD 拒掉是对的。反过来
  `energy_segments` 在这种数据上直接把整个 13 秒并成一段，没有可用性。
  **结论：现场链路必须保证 fsmn-vad 可用，不能指望能量兜底。**
- **切分偏碎，而且碎了会伤识别。** item_04 把唱票人一句话切成 3 段，识别成
  "张面飞梯""要再这样弄好好给你钱"；item_13 整段送识别反而能出
  "检查时间在面空带运营……"这种成句结果。SenseVoice 在短片段上明显更差，
  需要一个把相邻碎段合并回一次发言的后处理。
- **角色区分基本可用但不够。** 能量差明显时判得准（唱票人 x0.26~0.58，
  操作人 x1.43~1.88）；13 个 clip 里有 6 段落在 UNKNOWN 区（倍率接近 1）。
  item_11、item_12 整个 clip 都是未定。CAM++ 声纹校正还没实测（`--embedding`）。
- **识别质量是当前最大的问题，而且不只是同音字。** 领域词被整体音错：
  `10kV` → "时间伏" / "时间为" / "时间五"，`试验位置` → "时间位置"，
  `推至` → "推直"。这已经超出"同音字替换"的范畴，是跨音节混淆，
  纯拼音等价类未必兜得住，需要用设备台账做强制吸附。
- item_09 那段能量最高（x1.88）却输出空文本的问题，**是 fp16 autocast 造成的**，
  切回 fp32 后正常输出"一师段立祥呃，吉验开馆却在"。这是 autocast 除了更慢更
  占显存之外的第三个负面证据。

## 五、当时阻塞（2026-08-29，已解除）

> 丹东数据已到位，本节与接下来两节只作历史记录，勿据此判断现状。

`data/clips/` 下有 13 段已人工剪好的片段和 1 个完整录音 `_full_audio.wav`，
但 `data/tickets/ticket1.json` **不是这批音频对应的操作票**，无法做端到端校验验证。

需要提供：这批 clips 对应的真实操作票（JSON 或原始格式均可，格式不一致我来适配）。

P2 的分段与角色判定不依赖操作票，已经用 `scripts/inspect_audio.py` 单独验过，
结论见上一节。

## 六、下一步

不需要等票面：

1. 碎段合并：把同一个人连续的短语音段并回一次发言再送识别
2. 用设备台账做识别后吸附，缓解 `10kV` → "时间伏" 这类跨音节音错
3. 实测 `--embedding` 开启 CAM++ 后能不能压低 UNKNOWN 段占比

需要票面：

5. 补 `src/offline/batch_verify.py`：整段音频 + 完整操作票 → 逐条校验报告
6. 用 `_full_audio.wav` 验证顺序对齐状态机和闲聊过滤
7. P3 实时流式服务（`src/server/ws_server.py`），复用 P1/P2 内核
8. P4 阈值调优，目标函数是把漏报不合格压到接近零

## 七、还需要现场提供的材料

1. clips 对应的操作票（当前最阻塞的一项）
2. 确认现场是否有"对，执行"这类规程应答用语（听一段录音即可）
3. 设备台账 / 间隔命名全量列表 → 放到 `configs/asset_list.txt`
4. 推流设备的协议与音频参数（采样率、编码格式、分片大小）
5. 更多操作票样本，槽位规则需要更多句式才能覆盖全

## 八、版本管理

当前目录**不是 git 仓库**，所有进度只存在于本地文件。建议尽早 `git init`
并提交一次基线，否则配置阈值调坏了没法回滚。

## 九、识别后吸附（2026-09-13）

> 本节数字是当时（SenseVoiceSmall + 修复前的归一化）跑出来的。当前默认组合下
> 重跑的对照见 [README.md](README.md) 的「识别后吸附 A/B」，结论方向一致，
> 数值有变化。

补上第六节下一步里的第 2 项。新增 `src/normalize/adhere.py`，接在识别和匹配之间。

**做法**：拿站点台账全称和领域词表当靶子，在识别文本里用模糊音编辑距离找最接近
的片段，改写成靶子的标准写法。改写发生在匹配之前，而且**完全不看操作票** ——
只问"这个音最像词表里的哪个词"，不问"票面写的是什么"。真把 A 念成 B 时，B 本身
也是词表里的词，吸附不会把它改回 A，说错照样是说错。

**护栏**：

1. 被替换片段和写进靶子的数字必须一模一样。这条挡两个方向：`黄冈线310开关`
   不许变成 `黄堽线312开关`，`2号` 也不许变成 `良好`（两字同音，但数字没了）。
2. 通用设备后缀（开关/刀闸/母线）不参与吸附。修后缀会把听残的片段修得像真设备，
   `合尚丹东县也告3道闸` 里抽出个 `3刀闸`，就把"没听清"（灰区）冤枉成"说错了"
   （直接不通过）。丹东 ticket7 第 6 条实测踩过这个坑。
3. 阈值 0.9，每次改写留痕进报告（原文、靶子、相似度、来源类别）。

**实测**（`--no-adhere` 与开启各跑一遍）：

| 数据 | 吸附 | 通过 | 灰区 | 疑似说错 | 未确认 | 告警 |
|---|---|---:|---:|---:|---:|---:|
| 小留 ticket1 | 关 | 0 | 1 | 4 | 2 | 7 |
| 小留 ticket1 | 开 | 2 | 3 | 1 | 1 | 6 |
| 丹东 ticket7 | 关/开 | 0 | 2 | 4 | 3 | 7 |
| 丹东 ticket11 | 关/开 | 4 | 1 | 5 | 1 | 7 |

丹东逐条状态完全不变（只个别条目分数变化），小留恢复出第 1、3 条通过。小留第 2 条
仍是"疑似说错"且不该改：识别听到 310、票面是 312，到底是 ASR 听错还是人念错
系统分不出来，只能交人回听。

**仍然修不了的**：跨音节音错（`分闸`→`分查`、`装设`→`装饰`）。这些词要么不在词表
里，要么读音差出一个声母，0.9 阈值下拦得住误改也拦得住误救 —— 这个取舍是刻意的，
放宽到能救它们就会开始误改位置值。这类残留只能靠 CTC 定向复核，或者由现场补齐
词表（`装设` 目前就不在 `configs/lexicon.yaml` 的 actions 里）。

## 十、分段器 / 碎段合并 / 识别模型三变量 A/B（2026-09-13）

> **这一节是历史记录，不是现状。** 里面提到的所有开关（`--segmenter` /
> `--silence-ms` / `--merge` / `--fp16` / `--model-dir`）、Silero 分段器、
> 碎段合并、以及 `a9_*` / `x5_*` 这些对照标签，**都已经删除**。
> 现状见第十一节。这里保留的是当时的结论和推理过程。

起因是现场反馈的两条：VAD 会把一句话切成两段，两段单独匹配得分都低；以及
ASR 用的是最简模型，能不能升级（要一并考虑显存和推理时长）。

### 结论先行

| 变量 | 决定 | 依据 |
|---|---|---|
| ASR | **SenseVoiceSmall → Paraformer-Large**（220M，AISHELL-1 CER ~1.7%） | 三段录音上通过数都不低于原模型，RTF 与显存基本持平 |
| VAD | **保持 FSMN-VAD**（当时给 Silero 留了开关，第十一节已删） | Silero 在现场远场录音上往另一个方向错：底噪高、条目间停顿达不到静音判据，整条操作被并成一段 |
| 碎段合并 | **默认关闭**（原来是开的） | 它粘的是唱票和复述，一段文本里同一句出现两遍会被匹配器压低分 |
| fp16 | **继续关闭** | 换 Paraformer 后重测，两个模型上仍是负收益 |

换模型的直接收益不大；**换模型换出来的一个归一化 bug 才是这轮最大的收获**
（见下），修完之后 Paraformer 才真正全面不低于 SenseVoice。

### 三变量对照（通过/灰区/疑似说错/未确认）

| 配置 | 丹东 ticket11（11 条） | 丹东 ticket7（9 条） | 小留 ticket1（7 条） |
|---|---|---|---|
| FSMN + 不合并 + SenseVoice（旧基线） | 4/1/5/1 | 0/2/4/3 | 2/3/1/1 |
| **FSMN + 不合并 + Paraformer（当前默认）** | **4/2/5/0** | **1/1/4/3** | **3/3/1/0** |
| FSMN + 合并 + Paraformer | 0/8/3/0 | 2/0/3/4 | 1/6/0/0 |
| Silero@150 + 不合并 + Paraformer | 1/4/3/3 | 0/1/5/3 | 1/5/1/0 |

分段数（同一顺序）：FSMN 33/69/35，Silero@150 25/53/23，Silero@400 13/40/12。
完整表见 `benchmarks/results/{dandong,xiaoliu}/comparison.md`，由
`benchmarks/compare_runs.py` 从各次跑的 `report.json` 汇总生成
（替换掉了原来写死 baseline/optimized 两个标签的 `compare_dandong_runs.py`）。

### 负面结论一：Silero 在这个场景下反而更差

基准集上 Silero 的误报率远低于 FSMN（9.41% vs 44.03%），换它是冲着治碎段去的。
现场录音上它往**另一个方向**错：远场录音底噪高（90~145s 段 RMS 的 10 分位是
0.025、中位数 0.118），条目之间的停顿达不到 Silero 的静音判据，于是整条操作
被并成一段。丹东 ticket11 上它只切出 13 段，最长一段 **51 秒**，而那 51 秒里
含 5 条票面内容。

这条链路"切长"比"切短"代价大得多 —— 对齐器一段文本只认一条票
（`SequentialAligner.feed` 按窗口取单个最佳匹配），一段吃掉好几条内容时只有
一条能得分，其余落进「未确认」，那是连人工复核线索都没有的状态。三套录音上
Silero 的通过数每次都更低。

调小 `vad.silero.min_silence_duration_ms` 能缓解（ticket11：400ms→13 段、
150ms→25 段、100ms→31 段），但这个值和录音增益、底噪强相关，**换站点必须重调**，
不适合当全局默认。为此加了 `--segmenter` / `--silence-ms` 两个开关，
`configs/default.yaml` 的 `vad.model` 保持 `fsmn`。

### 负面结论二：碎段合并粘的是唱票和复述

合并逻辑本身没问题（`merge_segments` 是纯函数，判断分支都有测试）。问题在于
同一条票两个人各念一遍，中间只隔一次换气，正好满足合并条件。拼出来的文本里
同一句内容出现两遍，归一化后送进匹配器，自重复把得分压下去，干净通过变成灰区。
报告里的 `heard` 字段能直接看出来：

```text
不合并：检查黄堽线312开关机械位置指示确在分叉位置
合  并：检查黄堽线312开关机械位置指示确在分叉位置检查黄冈线312开关机械位置指示确在分叉位置对
```

小留 ticket1 上这一项把通过数从 3 压到 1、灰区从 3 涨到 6；丹东 ticket11
通过从 4 掉到 0、灰区涨到 8。丹东 ticket7 是唯一例外（通过 1→2），但未确认
同时从 3 涨到 4 —— 长段吃多条内容，代价换了个地方出现。综合看是净负，
`vad.merge.enabled` 默认改为 `false`，代码里 `apply_merge` 的兜底默认值也跟着
改成 `false`（两处不一致时，只传部分配置的调用方会得到与文档相反的默认行为）。

要重新启用它，得先让匹配器容忍一段文本里的自重复。

### 换 Paraformer 暴露出的归一化缺陷

第一轮换完 Paraformer，丹东 ticket11 的通过数从 4 掉到 1，看着像是新模型更差。
逐段比对识别文本定位到的是归一化层：

- 票面写 `1-1KLP2`，连字符本来就被 `_PUNCT` 丢掉 → `11klp2`；
- Paraformer 把这个位置念成一个字，输出 `一杠一KLP2`，而「杠」是汉字不是标点，
  被原样保留 → `1杠1klp2`。

同一个设备于是有了两个不同的串，设备槽位判缺失，条目得分从 0.892 掉到 0.543。
SenseVoice 恰好吐连字符，所以这个缺陷一直没暴露。

修法是在 `src/normalize/text_norm.py` 里把「杠」按连字符处理，**只在两侧都是
数字时丢**，避免误伤「杠杆」「双杠」「钢筋」这类正常词；且必须放在中文数字
转换**之前** —— 那一步按连续数字段切 run，"杠"会把它断成两半。

| 丹东 ticket11 | 通过 | 灰区 | 疑似说错 | 未确认 |
|---|---:|---:|---:|---:|
| 修前（`a9_pre_norm_fix`） | 1 | 2 | 7 | 1 |
| 修后（`a9_fsmn_nomerge_pf`） | **4** | 2 | 5 | **0** |

修完 Paraformer 在三段录音上就都不低于 SenseVoice 了。（当时留了一份修前的
报告 `a9_pre_norm_fix` 作对照，已随第十一节的清理删除。）对照用例进了
`tests/test_text_norm.py`，这是永久保留的那部分。

### 顺手修掉的另一个不一致：fp16 的代码默认值

`AsrEngine.__init__` 里 `fp16` 的代码默认值写的是 `True`，而
`configs/default.yaml` 写的是 `false`，注释和 `_inference_context` 的说明也都说
默认不开。带 `asr` 段的配置看不出差别，但 `AsrEngine({})` 这种不带 `asr` 段的
调用会静默走进 autocast —— 我第一次测显存就是这么测错的（测出 1539MB，
真实值 921MB）。已把代码默认值改成 `False` 与配置对齐。

### 性能（丹东 ticket7 261.4s / ticket11 159.5s，正常跑批路径）

| 模型 | 模式 | RTF | 峰值显存 |
|---|---|---:|---:|
| Paraformer-Large | fp32 | 0.0122–0.0152 | 921.1MB |
| Paraformer-Large | fp16 autocast | 0.0332–0.0368 | 1171MB |
| SenseVoiceSmall | fp32 | 0.0126–0.0161 | 977.2MB |
| SenseVoiceSmall | fp16 autocast | 0.0349–0.0407 | 1399MB |

峰值显存写进报告（`summary.peak_gpu_mb`，进程级高水位），上表可从产物复算，
不用另跑一套测量 —— 整段识别和按段识别的显存占用差得很多，两种测法不可比。
两个模型上 fp16 都是负收益：慢约 2.5 倍、显存还多占约 25%。权重仍以 fp32
常驻，autocast 要额外缓存一份 fp16 副本，而每次 `generate` 都是新的 autocast
上下文，转换开销付了却用不上缓存。`--fp16` 开关保留，只为让表里那两行可复算。

### 本轮改动的文件

| 文件 | 改动 |
|---|---|
| `src/normalize/text_norm.py` | 设备编号隔断符归一（「杠」按连字符处理，两侧须为数字） |
| `src/asr/engine.py` | `fp16` 代码默认值 True → False，与配置对齐 |
| `src/asr/vad_stream.py` | `apply_merge` 空字典不再回退到仓库配置；默认值改为关闭 |
| `src/offline/batch_verify.py` | 新增 `--segmenter`/`--silence-ms`/`--merge`/`--no-merge`/`--fp16`/`--model-dir`；报告写入 `peak_gpu_mb` |
| `configs/default.yaml` | 主模型换 Paraformer-Large（降级备用 SenseVoiceSmall）；`vad.model: fsmn`；`vad.silero.min_silence_duration_ms: 150`；`vad.merge.enabled: false` |
| `benchmarks/compare_runs.py` | 新文件，替换 `compare_dandong_runs.py`；自动发现标签，加段数/合并数/RTF 列 |
| `download_model.py` | 可同时拉 Paraformer-Large 与 SenseVoiceSmall |
| `tests/` | 新增归一化隔断符用例、`apply_merge({})` 回退用例；合并/Silero 的用例改成显式传配置 |

测试 94 项全绿。

### 下一步（按价值排序）

1. **让一段文本能推进多条票面。** 对齐器现在一段只认一条，这是 Silero 吃亏、
   合并默认关闭、以及长段里内容丢失的共同根因，也是当前最值得做的一处改动。
2. 让匹配器容忍一段文本里的自重复，之后碎段合并才有重新打开的可能。
3. ~~补上 Silero@400 + Paraformer 在丹东的对照组~~ —— Silero 已删，这一项作废。
4. CTC 定向复核 / CAM++ 正式评测 / WebSocket 实时服务（第三项一直没动）。

### 这些结论的置信度

全流程只有**三段录音、27 条票面**。每个变量只改一处的 A/B 已经做到，但这个
样本量决定了差异里有噪声：ticket7 上合并反而让通过数 +1，就是个反例。
**不要把这张表当成模型选型的确切依据**，换站点后所有对照都要重跑一遍。
三段录音都没有人工转写和条目时间戳，因此本文件里没有任何准确率数字，
只有系统输出分布。

## 十一、定为基线，其余方案删除（2026-09-13）

基线定为 **FSMN-VAD + 不做碎段合并 + Paraformer-Large fp32 + 识别后吸附**。
第十节里评估过的其他方案连同代码、配置、测试和实验报告一并删除 ——
留着结论比留着一堆默认关闭的开关有用。

### 删掉了什么

| 对象 | 内容 |
|---|---|
| `src/asr/vad_silero.py` | Silero 分段器整个模块 |
| `src/asr/vad_stream.py` | `merge_segments` / `apply_merge` 碎段合并、`build_segmenter` 工厂与降级链、`Segment.merged_from` |
| `src/asr/engine.py` | `fp16` 与 `_inference_context` 的 autocast 路径、`describe()` 里的精度后缀 |
| `src/offline/batch_verify.py` | `--segmenter` / `--silence-ms` / `--merge` / `--no-merge` / `--fp16` / `--model-dir`，报告里的合并字段 |
| `configs/default.yaml` | `vad.model` / `vad.silero` / `vad.merge` / `asr.fp16` 四段配置 |
| `requirements.txt` | `silero-vad` |
| `tests/` | `test_vad_silero.py`、`test_vad_merge.py` |
| `benchmarks/results/` | 除 `baseline` 外的全部跑批报告，以及 `comparison.json` / `comparison.md` |

删除让测试从 94 项降到 72 项（少的都是被删文件的测试），其余全绿。

保留的三样，都不是"竞争方案"：

- **识别后吸附**：链路的一环。`--no-adhere` 留着，用于它在新站点上误改时把
  原因单独摘出来。
- **SenseVoiceSmall 降级备用**：Paraformer 加载失败时的退路。没有它，跑批会在
  模型加载这一步直接停摆。
- **`energy_segments` 纯能量兜底**：没有网络下载 fsmn-vad 时至少能把长音频切开。

### 顺带修掉的

`AsrEngine.__init__` 和 `VadSegmenter.__init__` 里的 `config or load_config()`
与上一轮 `apply_merge` 是同一类问题：传 `{}` 会被当成"没传配置"，于是仓库里那份
yaml 悄悄接管，调用方以为自己什么都没配、实际按 yaml 走了。都改成只有 `None`
才回退。

### 代价与缺口

- 碎段合并删除后，**最初那条"VAD 把一句话切成两段"的反馈失去了专门的兜底
  手段**。现在靠的是 FSMN 的切分粒度尚可，以及 Paraformer 比 SenseVoice 更能
  应付短片段 —— 这条反馈本身仍然成立，只是被缓解了。
- 换站点不再有 `--silence-ms` 可调（那是 Silero 的参数）。换站点先跑
  `scripts/inspect_audio.py` 看 FSMN 的段数和最长段。
- 要重新评估任何一个方案，照第十节的描述重做一遍对照即可，比维护一堆默认
  关闭的开关便宜。
- README 里那张方案对照表**不再有可复算的出处**（报告已删），数字只存在于
  本文件。这是这次清理明确接受的代价。

## 十二、修 VAD 粒度错配造成的误判（2026-09-13）

现场看小留 ticket1 的报告提出的问题：识别本身已经不错了，但有两类误判卡在
切分粒度上，跟识别质量无关 ——

1. 一句话被 VAD 切成两段，两段各自拿去匹配，谁都不完整，条目进不了「通过」；
2. 唱票人和操作人的语音粘在同一段里，两份内容互相稀释，条目落进灰区。

第十一节删掉音频层的碎段合并时留了一句"这条反馈失去了专门的兜底手段"。
**兜底改在对齐层做，VAD 本身不动** —— 换 Silero、按静音间隔合并、按角色合并
三条路都在第十节测过并否掉了。

### 对齐器：内容不完整的段先挂起，等下一段拼合

`SequentialAligner.feed` 原来假设"一段文本 = 一条票面内容"，VAD 的粒度把这个
假设从两个方向都打破了。现在判定落在灰区的段先挂起（不推指针），下一段到了
拼起来重判一次；拼不成再按自己的结论落地。`finalize()` 冲刷最后一段。

采纳拼合的判据是**分数增益**，不是静音间隔也不是角色：

| 判据 | 为什么不行 |
|---|---|
| 按静音间隔合 | 第十节栽过。唱票和复述之间正好是"换一口气 + 同一句内容"，必然把两个人粘在一起 |
| 按角色合 | 角色判定本身不稳（`configs/default.yaml` 里写着） |
| 按内容合 | 唱票和复述的内容本来就是同一句，分不开 |

只有"拼起来确实更像一条完整票面"区分得开，而且它**自校验**：拼合后不涨分的
就不采纳，已经通过的那些段不会被改坏。实测增益分布：该拼的（同一句话被切成
上下半截）+0.38；勉强够格的 +0.07；唱票尾巴粘操作人头、闲聊两段相拼一律为负。
门槛取在 0.07 和 0.38 之间，即 `aligner.stitch_gain: 0.10`，另加两条硬门：
拼合结果判 FAIL 的一律不采纳，分数低于灰区下限的一律不采纳。

三条真实录音上的落地情况：小留 ticket1 的第 16+17 段以 0.935 被采纳（这是个
真拼合，报告里前一段记 `MERGED`、后一段的判读文本带「拼合」前缀）；第 26+27
段（0.717 + 0.115）和第 30+31 段（0.717 + 0.862）都因为涨分不够被拒。丹东两份
录音一次都没触发。

### 匹配器：矛盾只在有闭集可对照时才成立

"设备全称被听残成半个名字"被判成了 CONFLICT（设备说错），直接升格成疑似说错。
矛盾的正确定义是**念成了同组里的另一个值**，而设备号、电压、残余实词这些槽位
根本没有互斥取值可对照 —— 只有位置、对象、把手、动作、检查词有（词表里的
`group_values`）。所以这些槽位改判缺失，最高到灰区。设备编号那道数字闸门保持
不变：编号真说错（`111开关` → `121开关`）仍然直接判否。

顺带把无闭集槽位的比对从整串相似度改成子串包含。复述比票面长是常态（口语
填充、同一句说两遍、唱票和复述粘在一段），整串相似度按最长边归一化，多出来的
字全要扣分，一段正确复述带个"嗯"就能判不过。子串包含只按 needle 归一化，问的
是"票面这几个字有没有被念出来"，才是这个槽位要问的事。

### 安全洞：`position` 进必要槽位

`matcher.required_slots` 增加 `position`。票面「确在**分闸**位置」说成「确在
**合闸**位置」原本得分 0.881、**判 PASS 且无告警** —— 一条 reason 都没有拦住
它。这违反 [verification_logic.md](docs/verification_logic.md) §4 F1（方向说反
必须拦）和 §8（漏报更不能接受）。`position` 是兜底槽位，管的是 src/dst 之外的
那个位置值。

### 疑似说错可以降级为灰区

条目已被判 FAILED 时，如果后面跟的复述**得分接近通过、又没有矛盾槽位**，改判
灰区交人工听录音。得高分却判灰区只可能是某个必要要素没听全，这时再报"疑似
说错"就是拿识别噪声当现场错误。有矛盾槽位的不给这条路 —— 矛盾才是"说错"的
正证据。小留第 2 条就是这条路径：第 15 段把设备编号说成 310（真说错），但后面
16+17 拼合出的完整复述得 0.935，只是"分闸"没听出来。

### 试过但没采纳：强制吸附表

`装饰→装设`、`接力线→接地线`、`却在→确在` 这三个听错词相似度够不到 0.9 的
吸附阈值，本来打算加一张精确替换的 `adhere.forced` 表。**逐条量过之后放弃了**：
没有任何一条的判定依赖于这三处改写，加了只会多一条需要维护的路径。
**「分叉→分闸」明确不加**：位置值是安全关键槽位，"位置值不自动改写"是已有
测试确立的原则，靠上面那条"降级为灰区"交给人工回听才对。

### 结果

| 数据 | baseline | 当前 | 变化的条目 |
|---|---|---|---|
| 小留 ticket1 | 3 通过 / 3 灰 / 1 说错 / 0 未确认，告警 5 | **5 / 2 / 0 / 0**，告警 8 | 2 说错→灰、5 灰→通过、7 灰→通过 |
| 丹东 ticket11 | 4 / 2 / 5 / 0，告警 8 | **5 / 3 / 3 / 0**，告警 6 | 2 说错→灰、4 说错→灰、10 灰→通过 |
| 丹东 ticket7 | 1 / 1 / 4 / 3，告警 9 | 不变 | —— |

段数完全没有变（35 / 33 / 69），因为这轮改的全是对齐和匹配。ticket11 剩下的
三条疑似说错都是真的：`110kV` 说成 `120kV`、`111开关` 说成 `11开关` 和
`121开关`。

小留两条灰区（第 2、4 条）都不是"没救回来"，而是**真的没听出"分闸"**（识别成
"分叉"）。这一格必须留给人回听，见上面为什么不做强制吸附。

### 代价

- **告警数在两条录音上反向变化**：小留 5→8，ticket11 8→6。小留多出来的是两条
  中间态告警（条目 1 和第 5 条先报了疑似说错、后面又被清晰复述纠正为通过）。
  最终状态是好的，但现场会看到"报了又撤"。要不要把中间态告警压到只记日志、
  不进告警栏，是个独立的口径问题，这一轮没动。
- 挂起段的结论要**延迟一段才输出**。离线跑批零影响；将来接实时链路时，这段时间
  大约是一个 VAD 段的时长。现在没有实时模块，先记着。
- RTF 只有同一台机器同一段时间内跑出来的才能横着比。同一份音频、同一套配置，
  baseline 那份报告是 0.011、这次重跑是 0.051，每段固定开销差了 4.5 倍（0.057s
  → 0.26s），和改动无关（`total_infer_seconds` 只累加 `generate` 的耗时，这轮
  没碰 ASR 路径）。RTX 4060 Laptop 的功耗/温度状态就能造出这个差。比较表里
  这一列**不要当性能回归读**。
- 三段录音仍然没有人工转写和条目时间戳，所有结论都只是系统输出分布。

### 新增的工具和测试

| 对象 | 内容 |
|---|---|
| `benchmarks/replay_report.py` | 读既有 report.json 的逐段 ASR/吸附文本，用当前代码重跑对齐，对比条目状态。改判定逻辑时不必重跑 ASR，也能看出"一涨一跌抵消"这种总数不变的变化。退出码非 0 表示有状态变化 |
| `src/offline/batch_verify.py` | 逐段的行改为对齐跑完后再拼 —— 挂起的事件对象会被原地改写，循环里就地取会拿到过期状态；`MERGED` 行和「拼合」文本在报告里显式标出 |
| `tests/test_aligner.py` | 拼合升级为通过、增益不够时拒绝拼合、拼合不许把说反了拼成通过、疑似说错降级灰区、挂起段的最终结论（`finalize` 冲刷、事件数一段一行） |
| `tests/test_matcher.py` | 位置值说反判否、设备全称听残判缺失而非矛盾、设备编号真说错仍判矛盾 |

测试从 72 项加到 80 项，全绿。三份报告都用 `replay_report.py` 回放验证过，
重放结果与报告里记的条目状态逐条一致。
